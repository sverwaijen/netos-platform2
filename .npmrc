# STANDARDIZED ON PNPM
# This project uses pnpm as the package manager (see packageManager in package.json)
# DO NOT use npm or yarn to install dependencies as it may cause conflicts
# Always use: pnpm install
# See package-lock.json is legacy from earlier setup and should not be used

shamefully-hoist=true
strict-peer-dependencies=false
